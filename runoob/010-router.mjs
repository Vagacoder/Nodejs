/*
 * node js simple router  
 * using with 009-server.mjs and 011-index.mjs 
 */

 const route = (pathname) => {
     console.log(`About to rout a request for ${pathname}`);
 }

 export default route;